[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "SAP-HR-Finance-ETL-Pipeline"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["pipelines*"]
